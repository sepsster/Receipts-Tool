[project]
name = "receipts-tool"
version = "0.3.2"
description = "Portable local receipt generator."
requires-python = ">=3.11"
dependencies = [
  "Pillow",
  "reportlab",
]

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
